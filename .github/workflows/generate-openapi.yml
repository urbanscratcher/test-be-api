name: Generate OpenAPI YAML

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'src/**'
      - 'build.gradle'
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:  # 수동 실행 가능

jobs:
  generate-openapi:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 커밋 및 푸시 권한
      pull-requests: read  # PR 읽기 권한
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0  # 전체 히스토리 가져오기
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      
      - name: Sync with remote before generation
        if: github.event_name != 'pull_request'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          git config --local user.email "urbanscratcher@github.com"
          git config --local user.name "Joun"
          
          # 원격 변경사항 가져오기
          git fetch origin $BRANCH_NAME || true
          
          # 원격과 동기화 (충돌 시 현재 브랜치 유지)
          git merge origin/$BRANCH_NAME --no-edit || git rebase origin/$BRANCH_NAME || true
      
      - name: Generate OpenAPI YAML
        run: |
          chmod +x scripts/generate-openapi.sh
          ./scripts/generate-openapi.sh
      
      - name: Check for changes
        id: verify-changed-files
        run: |
          if [ -n "$(git status --porcelain api-contracts/)" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi
      
      - name: Commit and push if changed
        if: steps.verify-changed-files.outputs.changed == 'true' && github.event_name != 'pull_request'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          
          # 변경사항 커밋
          git add api-contracts/
          git commit -m "chore: 자동 생성된 OpenAPI YAML 업데이트 [skip ci]" || exit 0
          
          # 푸시 (실패 시 재시도)
          MAX_RETRIES=5
          RETRY_COUNT=0
          
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            # 원격 변경사항 다시 확인
            git fetch origin $BRANCH_NAME || true
            
            # 로컬이 원격보다 앞서있는지 확인
            LOCAL=$(git rev-parse HEAD)
            REMOTE=$(git rev-parse origin/$BRANCH_NAME 2>/dev/null || echo "")
            
            if [ -n "$REMOTE" ] && [ "$LOCAL" != "$REMOTE" ]; then
              # 원격에 새로운 커밋이 있으면 rebase
              echo "원격에 새로운 변경사항 발견. rebase 중..."
              git rebase origin/$BRANCH_NAME || {
                echo "rebase 실패. merge 시도..."
                git merge origin/$BRANCH_NAME --no-edit || {
                  echo "merge도 실패. 강제로 재생성된 YAML 사용"
                  git checkout --ours api-contracts/ || true
                  git add api-contracts/
                  git commit -m "chore: 자동 생성된 OpenAPI YAML 업데이트 [skip ci]" || true
                }
              }
            fi
            
            # 푸시 시도
            if git push https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git HEAD:$BRANCH_NAME; then
              echo "✅ 푸시 성공"
              exit 0
            else
              RETRY_COUNT=$((RETRY_COUNT + 1))
              echo "⚠️ 푸시 실패. 재시도 $RETRY_COUNT/$MAX_RETRIES"
              
              if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
                sleep 3
              fi
            fi
          done
          
          echo "❌ 푸시 실패: 최대 재시도 횟수 초과"
          exit 1
      
      - name: Show changes in PR
        if: steps.verify-changed-files.outputs.changed == 'true' && github.event_name == 'pull_request'
        run: |
          echo "## 📝 OpenAPI 스펙 변경사항" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "이 PR에서 OpenAPI 스펙이 변경되었습니다. 리뷰 후 머지 시 자동으로 업데이트됩니다." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          git diff api-contracts/ >> $GITHUB_STEP_SUMMARY || true
      
      - name: Upload OpenAPI artifacts
        uses: actions/upload-artifact@v4
        with:
          name: openapi-specs
          path: api-contracts/
          retention-days: 30

